/** @param {NS} ns */
export async function main(ns) {
  growthAnalyze(host, multiplier, cores) = /**Calculate the number of grow 
  threads needed for a given multiplicative growth factor.**/

  growthAnalyzeSecurity(threads, hostname, cores) = /**Calculate the security 
  increase for a number of grow threads."**/

  hackAnalyzeSecurity(threads, hostname) = /**Get the security increase 
  or a number of threads.**/

  hackAnalyzeThreads(host, hackAmount) = /**Calculate the decimal number of 
  threads needed to hack a specified amount of money from a target host.**/

  weakenAnalyze(threads, cores) = /**Predict the effect of weaken.**/

}